pyensembl>=1.5.0
# Varcode 10 is the tested major version (CI and the suite run on 10.x).
# 10.5.2 honors an Ensembl release in --genome (GRCh38:93 or GRCh38.93) instead
# of silently using the latest release.
varcode>=10.5.2,<11
pandas>=0.23.0
pysam>=0.15.2
psutil
edlib>=1.3.9.post1,<2

# Test reads (the openvax-v1 bundle) and regional acquisition for research audits.
osteosarc>=0.11.1,<0.12
