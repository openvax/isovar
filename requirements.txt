pyensembl>=1.5.0
# Varcode 10 is the tested major version (CI and the suite run on 10.x).
# 10.5.0 honors an Ensembl release in --genome (GRCh38:93) instead of silently
# using the latest release.
varcode>=10.5.0,<11
pandas>=0.23.0
pysam>=0.15.2
psutil
edlib>=1.3.9.post1,<2

# Shared fixture mechanics (including offline historical SAM bundles).
osteosarc>=0.2.3,<0.3
